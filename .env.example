# URL base de la API. En producción la PWA y la API comparten dominio vía CloudFront
# (behavior /api/*), así que la ruta relativa es lo correcto.
VITE_API_URL=/api

# Clave pública VAPID para Web Push (la privada vive solo en el backend).
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
